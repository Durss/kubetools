package com.muxxu.xmlapi {	import com.muxxu.xmlapi.events.MuxxuAPIEvent;
	import flash.display.MovieClip;	/**	 * Bootstrap class of the application.	 * Must be set as the main class for the flex sdk compiler	 * but actually the real bootstrap class will be the factoryClass	 * designated in the metadata instruction.	 * 	 * @author Francois	 */	 	[SWF(width="300", height="300", backgroundColor="0xFFFFFF", frameRate="31")]	public class MuxxuAPITester extends MovieClip {										/* *********** *		 * CONSTRUCTOR *		 * *********** */		/**		 * Creates an instance of <code>Application</code>.<br>		 */		public function MuxxuAPITester() {			initialize();		}						/* ***************** *		 * GETTERS / SETTERS *		 * ***************** */		/* ****** *		 * PUBLIC *		 * ****** */						/* ******* *		 * PRIVATE *		 * ******* */		/**		 * Initialize the class.<br>		 */		private function initialize():void {			MuxxuAPI.getInstance().initialize("kubetools", "a7c5135052fff132c41c68b639bdd02a", "89", "450cb0c6");			MuxxuAPI.getInstance().addEventListener(MuxxuAPIEvent.LOAD_DATA_COMPLETE, loadDataCompleteHandler);			MuxxuAPI.getInstance().addEventListener(MuxxuAPIEvent.LOAD_DATA_ERROR, loadDataErrorHandler);						MuxxuAPI.getInstance().getInformations(MuxxuAPI.USER);		}		private function loadDataCompleteHandler(event:MuxxuAPIEvent):void {			trace("MuxxuAPITester.loadDataCompleteHandler(event)");
		}
		private function loadDataErrorHandler(event:MuxxuAPIEvent):void {			trace("MuxxuAPITester.loadDataErrorHandler(event)");
		}	}
}